
/**
* Container class. Represents a set of other elements. Helper class. Used only as a result from Establishment.storedFoodUnits.<br/><br/>
* Loop through a Set the JavaScript way:<br/>
* <br/>
* theSet.each(function(element) {<br/>
* &nbsp;&nbsp;console.log(element);<br/>
* });<br/>
* <br/><br/>
* Loop through a Set the Java way:<br/>
* <br/>
* var it = theSet.iterator();<br/>
* while(it.hasNext()) {<br/>
* &nbsp;&nbsp;var element = it.next();<br/>
* &nbsp;&nbsp;console.log(element);<br/>
* }<br/>
* @class Set
* @constructor
*/
function Set() {
	
}

/**
* True if no elements are inside the Set. Read-Only
* 
* @property empty
* @type {Boolean}
*/
Set.prototype.empty;

/**
* Returns the number of elements inside the Set
*
* @method size
* @return {Number} Number of elements
*/
Set.prototype.size = function() {
	
};

/**
* Returns an iterator object to loop over the Set
*
* @method iterator
* @return {Iterator} iterator object
*/
Set.prototype.iterator = function() {
	
};

/**
* JavaScript-like callback to loop over all items
* 
*
* @method each
* @param {Function} forEachElement Will be called for each available items. Needs to have one parameter.
*/
Set.prototype.each = function() {	
};

