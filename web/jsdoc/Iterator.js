

/**
* Used to loop over a list/set of elements. Helper class.
*
* @class 
* @constructor
*/
function Iterator() {

	/**
	* Checks for another element
	*
	* @method 
	* @return {Boolean} True if there is another element available else false
	*/
	this.hasNext = function() {
	}

	/**
	* Returns the next element, must not be called if there isn't another element
	*
	* @method 
	* @return {Object} the next element
	*/
	this.next = function() {
	}

}

